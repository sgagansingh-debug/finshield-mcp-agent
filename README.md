# finshield-mcp-agent
FinShield is a privacy-first expense audit engine built on the Google ADK. It features a local zero-trust security gate that masks PII/PCI data before processing. The system uses a standard-compliant FastMCP Server to dynamically fetch pricing data from decoupled assets, allowing users to hot-swap Gemini models via the UI.
